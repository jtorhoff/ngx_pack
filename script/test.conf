events {
  worker_connections 4;
}

daemon on;
error_log /dev/stdout info;

http {
  access_log ./access.log;
  error_log ./error.log;

  gzip on;
  gzip_comp_level 1;
  gzip_types text/plain text/css;

  pack_zstd on;
  pack_zstd_level 1;
  pack_zstd_types text/plain text/css;
  # The fixtures here are a couple of dozen bytes; this suite is about
  # Accept-Encoding parsing, not the length threshold, so keep the old
  # default rather than the compiled-in 256.
  pack_zstd_min_length 20;

  server {
    listen 8080 default_server;
    listen [::]:8080 default_server;

    root ./;

    index index.html;

    location / {
      try_files $uri $uri/ =404;
    }
  }
}
